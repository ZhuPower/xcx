function getImgSrc(str){
  var src = ''
  console.log(str)
  if(str.indexOf('http://') == 0 || str.indexOf('https://') == 0){
    src = str
  }else{
    src = 'https://aa.ahy1.top' + str
  }
  
  return src;
}

module.exports = {
  getImgSrc:getImgSrc
}